MONGODB_URI=mongodb://localhost:27017/blockpage411
NEXT_PUBLIC_APP_URL=http://localhost:3000

# RPC and provider API keys (set these in production, keep them out of git!)
ETH_RPC_URL=
POLYGON_RPC_URL=
BSC_RPC_URL=
ETHERSCAN_API_KEY=
POLYGONSCAN_API_KEY=
BSCSCAN_API_KEY=
ALCHEMY_API_KEY=
INFURA_PROJECT_ID=

# Redis / Queue (optional)
REDIS_URL=redis://localhost:6379

# Admin wallets (comma-separated) used for simple admin header auth
ADMIN_WALLETS=

# Optional: Sentry DSN to enable error reporting
SENTRY_DSN=

# Operational
NODE_ENV=development
PORT=3000
