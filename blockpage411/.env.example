MONGODB_URI=mongodb://localhost:27017/blockpage411
MONGODB_DB=blockpage411

NEXT_PUBLIC_APP_URL=http://localhost:3000
BASE_URL=http://localhost:3000

# JWT / auth
JWT_SECRET=replace-with-strong-secret

# RPC and provider API keys (set these in production, keep them out of git!)
ETH_RPC_URL=
POLYGON_RPC_URL=
BSC_RPC_URL=
SOLANA_RPC_URL=
TRON_NODE_URL=
ETHERSCAN_API_KEY=
POLYGONSCAN_API_KEY=
BSCSCAN_API_KEY=
ALCHEMY_API_KEY=
INFURA_PROJECT_ID=

# Redis / Queue (optional)
REDIS_URL=redis://localhost:6379

# Admin wallets (comma-separated) used for simple admin header auth
ADMIN_WALLETS=
NEXT_PUBLIC_ADMIN_WALLETS=
NEXT_PUBLIC_ADMIN_ADDRESS=

# Rate / risk tuning
BALANCE_FLAG_THRESHOLD=5
HEAVY_FLAG_THRESHOLD=10
ADMIN_ACTION_RETENTION_DAYS=365
SLACK_MAX_ATTEMPTS=3
SLACK_BACKOFF_MS=500
ALERT_RETRY_BATCH=10
DEFAULT_TAX_RATE=0

# Poller / background
POLLER_SECRET=
PUBLISH_URL=http://localhost:3000/api/realtime/publish
POLLER_SLACK_WEBHOOK=

# Slack / alerts
SLACK_WEBHOOK_URL=

# Sentry
SENTRY_DSN=

# GivingBlock integration
GIVINGBLOCK_API_KEY=
GIVINGBLOCK_BASE_URL=https://public-api.sandbox.thegivingblock.com
NEXT_PUBLIC_GIVINGBLOCK_BASE_URL=https://public-api.sandbox.thegivingblock.com
GIVINGBLOCK_WEBHOOK_SECRET=
GIVINGBLOCK_ENCRYPTION_KEY=
GIVINGBLOCK_ENCRYPTION_IV=
GIVINGBLOCK_CACHE_TTL=21600

# Realtime / landing page
# Used by the live blockchain feed (components/LiveFeed/LiveFeed.tsx)
# Ethereum mainnet WSS: wss://eth-mainnet.g.alchemy.com/v2/${NEXT_PUBLIC_ALCHEMY_KEY}
# Polygon mainnet WSS:  wss://polygon-mainnet.g.alchemy.com/v2/${NEXT_PUBLIC_ALCHEMY_KEY}
NEXT_PUBLIC_ALCHEMY_KEY=
# Optional BNB Chain WSS endpoint from your node provider
NEXT_PUBLIC_BSC_MAINNET_WSS=
NEXT_PUBLIC_SOLANA_RPC_URL=
NEXT_PUBLIC_HELIUS_KEY=

# Misc scripts / tests
INTEGRATION_BASE=http://localhost:3000
PROMOTE_MIN_REPORTS=50
PROMOTE_MIN_UNIQUE=5
OUTREACH_MIN_REPORTS=100
OUTREACH_MIN_UNIQUE=10
DOTENV_CONFIG_PATH=.env.local

# Operational
NODE_ENV=development
PORT=3000
