"use client";

import React from "react";

// Stub component kept to avoid unused import errors after removing
// the experimental WalletConnect v2 + ethers.js demo. This component
// is not used anywhere in the app and intentionally renders nothing.

export default function WalletConnectButton() {
  return null; 
}
         