
// This file is intentionally minimal.
//
// Historical note:
// A standalone demo WebSocket server previously lived here, but the
// production architecture now uses only Next.js + client-side providers
// (see `components/LiveFeed/LiveFeed.tsx`).
//
// Keeping this stub avoids suggesting a custom Node server in Vercel.

export {}; // no-op module
